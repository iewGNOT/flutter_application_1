name: life_gacha
description: Offline-first productivity RPG gacha MVP foundation.
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  crypto: ^3.0.6
  google_fonts: ^6.2.1
  cupertino_icons: ^1.0.8
  drift: ^2.28.1
  flutter_riverpod: ^3.3.1
  flutter_secure_storage: ^10.0.0
  go_router: ^17.1.0
  path: ^1.9.1
  path_provider: ^2.1.5
  sqlite3: ^2.9.4
  sqlcipher_flutter_libs: ^0.6.6
  uuid: ^4.5.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.7.1
  drift_dev: ^2.28.1
  flutter_lints: ^6.0.0

dependency_overrides:
  source_gen: ^4.2.2

flutter:
  uses-material-design: true
