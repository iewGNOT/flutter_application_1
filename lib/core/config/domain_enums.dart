enum TaskCategory { study, exercise, deepWork, creative, general }

enum TaskStatus { active, completed, archived }

enum FocusSessionStatus { active, paused, completed, failed, cancelled }

enum WalletEventType { focusSessionCompleted, gachaDrawSpent, manualAdjustment }

enum RewardRarity { white, purple, golden, red }

enum RewardCardStatus { available, drawn, redeemed, archived }

enum GachaDrawType { single, ten }

enum AchievementType {
  completedTasks,
  completedFocusSessions,
  accumulatedPoints,
  streak,
  characterLevel,
}
